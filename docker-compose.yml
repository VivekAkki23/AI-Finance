version: '3.8'

services:
  app:
    build: .
    container_name: ai-finance-dashboard
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - ./data.db:/app/data.db
    restart: unless-stopped